# Niveau 1 — Bases

## Exercice 1.1 — Hello, dynamique

Déclare une variable `name` (str) et une variable `age` (int), puis affiche `"Bonjour <name>, tu as <age> ans."` avec une f-string.

## Exercice 1.2 — Coercition piège

Sans exécuter le code, prédis le résultat de chaque ligne, puis vérifie :

```python
print("5" + "3")
print(int("5") + 3)
print("5" + 3)
```

Explique en une phrase pourquoi la troisième ligne se comporte différemment de PHP/JS.

## Exercice 1.3 — Liste et boucle

Déclare une liste `scores` contenant 5 notes (`int`). Calcule et affiche leur moyenne avec une boucle `for` classique (sans compréhension).

## Exercice 1.4 — Dict et f-string

Déclare un dict `person` avec les clés `name` et `age`. Affiche `"<name> a <age> ans"` en accédant aux valeurs du dict dans une f-string.

## Exercice 1.5 — Match statement

Écris une fonction `day_type(day)` qui retourne `"Week-end"` pour 6 et 7, `"Semaine"` pour les autres, en utilisant `match`/`case`.
